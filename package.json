{
  "name": "fondation-web-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cd fondation && bun run dev:web",
    "dev:all": "cd fondation && bun run dev",
    "dev:worker": "cd fondation && bun run dev:worker",
    "build": "cd fondation && bun run build",
    "lint": "cd fondation && bun run lint",
    "typecheck": "cd fondation && bun run typecheck",
    "format:check": "cd fondation && bun run format:check",
    "format:write": "cd fondation && bun run format:write",
    "install": "cd fondation && bun install",
    "postinstall": "echo 'Dependencies installed in fondation monorepo'"
  },
  "devDependencies": {},
  "dependencies": {}
}